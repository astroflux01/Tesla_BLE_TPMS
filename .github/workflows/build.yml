name: Build Tesla BLE TPMS Firmware

on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Arduino CLI
        uses: arduino/setup-arduino-cli@v1
        with:
          version: "0.x"

      - name: Verify Arduino CLI
        run: arduino-cli version

      - name: Install ESP32 Core
        run: |
          arduino-cli config init
          arduino-cli config add board_manager.additional_urls https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json
          arduino-cli core update-index
          arduino-cli core install esp32:esp32@3.3.8

      - name: List files
        run: ls -la

      - name: Rename sketch file to match folder
        run: mv Tesla_BLE_TPMS_v03.ino Tesla_BLE_TPMS.ino

      - name: Compile Firmware
        run: |
          arduino-cli compile --fqbn esp32:esp32:esp32:PartitionScheme=huge_app . --output-dir ./build

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: tpms-firmware
          path: |
            ./build/Tesla_BLE_TPMS.ino.bin
            ./build/bootloader.bin
            ./build/partitions.bin
